server:
  port: 8086

spring:
  application:
    name: shop
  datasource:
    url: jdbc:postgresql://localhost:5432/shop
    username: postgres
    password: 1004
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  multipart:
    max-file-size: 10MB
    max-request-size: 20MB
  security:
    user:
      name: none
      password: none

file:
  upload-dir: /root/uploads